import {NavbarCloseDirective} from './navbar-close.directive';

describe('NavbarCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarCloseDirective();
    expect(directive).toBeTruthy();
  });
});
